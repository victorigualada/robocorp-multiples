describe('System test', () => {
  describe('When reading a file', () => {
    it('should output the correct data', () => {
      // Didn't have time to implement this test, it should test the entire system, passing a mock file
      // and reading the output file to check if the data is as expected.
    });
  });
});
